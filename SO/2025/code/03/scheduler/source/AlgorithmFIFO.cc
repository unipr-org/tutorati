#include "AlgorithmFIFO.hh"

void AlgorithmFIFO::sort(std::list<IProcess*>& processes) {
    // rimozione del warning relativo al passaggio di un parametro non utilizzato
    (void)processes;
    return;
}
