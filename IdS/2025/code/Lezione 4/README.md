Per motivi di semplicità si utilizzerà un solo container con tutto all'interno.
Se si volesse qualcosa di più strutturato sarebbe ottimale usare un container
per servizio.

Solitamente si forniscono 2 tipi di dockerfile/docker-compose:
- sviluppo
- produzione
